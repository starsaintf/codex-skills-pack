# Provenance resolution run

This branch was created to resolve every metadata-only catalogue entry against the exact upstream sources pinned in `upstream_sources.json`.

The generated changes must pass strict provenance validation and the full cross-platform test suite before merge.
